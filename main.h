#ifndef MAIN
#define MAIN

#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <stdarg.h>

void _printstring(const char *s);
void _printchar(char c);
int _printf(const char *format, ...);

#endif
